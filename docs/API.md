# Referencia rápida de funciones

## `kali-tools/utils.py`
- `ensure_dirs()`
- `is_critical_command(command)`
- `confirm(message)`
- `append_log(path, text)`
- `run_command(command, require_confirmation=True, timeout=900)`
