# Changelog

## 2026-09-03

- Corrección de `instalar.sh` para entornos Kali con PEP 668 (`--break-system-packages`).
- Ampliación de `requirements.txt` con `colorama` y `tabulate`.
- Mejora de wrappers `sqlmap`, `metasploit`, `password-cracker` y `network-recon` con reportes JSON y soporte IA.
- Mejora del arranque de Open WebUI y del stack completo.
